# 《梦幻跑跑》游戏介绍说明

## 游戏简介

《梦幻跑跑》是一款基于 Three.js 开发的浏览器 3D 跑酷派对游戏。玩家在云海赛道上高速奔跑，通过跳跃、二段跳、角色技能和道具完成收集或竞速目标。

游戏采用单文件 `index.html` 作为主要客户端，无 npm、无构建流程；角色模型、Three.js 和 GLTFLoader 等资源存放在 `assets/`。支持单人 PvE、同屏双人竞速，以及局域网和在线房间 1v1 对战。

- 在线游玩：<https://dreamdash-arch.github.io/DreamDash/>
- 项目仓库：<https://github.com/DreamDash-arch/DreamDash>

## 核心特色

- 高速纵版跑酷：赛道长约 6300 世界单位，包含九个区域和多个检查点。
- 云海空中路线：主路之外有云桥、高空赛道和阶梯云平台，可二段跳或三段跳登入。
- 两种跑酷玩法：单人模式包含任务、收集、小怪弹幕和心形血量；双人模式以先到终点者获胜。
- 四名 KayKit 角色：骑士、蛮人、游侠和法师，各有独立模型、武器和技能。
- 角色强化成长：使用星币解锁每个角色的三级强化，单人、同屏双人和联机均可生效。
- 丰富道具系统：九种增益、四种陷阱和问号攻击道具，支持随机性和互相干扰。
- 自动分屏：同屏双人距离拉开时切换左右分屏镜头，靠近后恢复共享镜头。
- 多端联机：支持本机 Python 局域网服务，也支持 Cloudflare Workers + Durable Objects 在线中继。

## 游戏模式

### 单人模式

单人模式采用 PvE 跑酷规则：

1. 在主页点“单人模式”。
2. 选择小憩、沉梦、幻梦或噩梦难度。
3. 选择角色后进入倒计时。
4. 沿赛道收集物品、躲避障碍和小怪弹幕，到达终点结算。

每局会随机获得“收集 5 颗星星”“找到 3 个发光蘑菇”或“收集 8 枚金币”之一作为任务。完成后且收集至少 45 颗星星、没有阵亡，可获得 A 级评价；其余完整跑完的情况评为 B 级。两种结果都不惩罚失败。

### 同屏双人竞速

两名玩家共享同一屏幕和赛道，先到终点者获胜。落后玩家超过一定距离后会获得少量追赶速度；距离进一步拉开时，镜头自动切换为左右分屏。

### 局域网 / 在线 1v1

联机采用主机权威同步：主机运行完整物理逻辑，客机上传本机按键并渲染主机同步的状态。

- 在线房间：一侧点“创建房间”，另一方打开带房间码的链接即可加入，不需要安装软件。
- 局域网直连：在主机电脑运行 `python3 server.py`，双方连接终端显示的局域网地址。
- 联机双方各自使用本机保存的角色强化等级。
- 联机收集数据不写入单人累计星光，但会为本机玩家发放对应的星币。

## 操作方式

### 单人和同屏双人

| 操作 | P1 | P2 |
|---|---|---|
| 左右移动 | `A` / `D` | `←` / `→` |
| 跳跃 | `W` 或 `Space` | `↑` |
| 角色技能 | `Q` | 小键盘 `0` |
| 问号道具 | `S` | `↓` |
| 暂停 | `ESC` | `ESC` |

### 局域网 / 在线联机

两台电脑均使用同一套键位控制自己电脑上的角色：

| 操作 | 按键 |
|---|---|
| 左右移动 | `A` / `D` |
| 跳跃 | `W` 或 `Space` |
| 角色技能 | `Q` |
| 问号道具 | `S` |

## 单人难度

| 难度 | 速度倍率 | 障碍 | 小怪上限 | 单轮弹幕 | 心数 | 受击无敌帧 |
|---|---:|---|---:|---:|---:|---:|
| 小憩 | ×1.7 | 45%，双倍数量 | 2 | 2 | 5 | 2.0 秒 |
| 沉梦 | ×2.0 | 85%，双倍数量 | 4 | 6 | 3 | 1.2 秒 |
| 幻梦 | ×2.4 | 全开，双倍数量 | 8 | 12 | 3 | 0.9 秒 |
| 噩梦 | ×2.7 | 全开，双倍数量 | 12 | 18 | 3 | 0.7 秒 |

小怪会根据玩家速度计算提前量，并在玩家接近时发射扇形弹幕。心归零后会回到最近检查点，恢复满心，同时保留收集和任务进度。

## 角色与技能

### 单人模式技能

| 角色 | 技能 | 效果 |
|---|---|---|
| 骑士 | 剑气 | 向前释放可穿透剑气，摧毁沿途障碍和小怪。 |
| 法师 | 无敌 | 获得 6 秒无敌，免疫弹幕并穿过障碍效果。 |
| 蛮人 | 变大 | 变大至 500% 且无敌，接触障碍或小怪即可碾压；碾过 3 只小怪恢复 1 颗心。 |
| 游侠 | 翻滚 | 向当前方向翻滚，可用左右方向键调整滚动路线。 |

骑士剑气、蛮人碾压等每累计摧毁 3 个障碍或小怪，会转换为 3 枚金币并计入任务进度。

### 双人和联机技能

| 角色 | 技能 | 效果 |
|---|---|---|
| 骑士 | 挥砍 | 近距离命中对手并造成大量击退。 |
| 法师 | 变羊 | 发射追踪法球，命中后把对手变成小羊并定在原地。 |
| 蛮人 | 变大 | 体型变为 500%，推挤和碰撞力量大幅提高。 |
| 游侠 | 翻滚 | 向前翻滚突进，可用于躲避和冲撞。 |

## 角色强化

每个角色拥有三级永久强化，必须依次购买：

| 等级 | 费用 | 点满后的冷却倍率 |
|---|---:|---:|
| Lv.1 | 30 星币 | 原始冷却 ×1.8 |
| Lv.2 | 70 星币 | 原始冷却 ×1.6 |
| Lv.3 | 150 星币 | 原始冷却 ×1.4 |

所有角色技能的基础冷却在本版本中按原始数值乘 2，强化可以逐步降低冷却。

- 星币与累计星光分开保存；强化只消耗星币，不会减少历史累计星光。
- 旧存档第一次进入新版本时，会按已有累计星光自动补发一次星币。
- 强化永久自动生效，不需要额外装备或切换。
- 单人、同屏双人、局域网和在线联机均可使用角色强化。

### 各角色强化方向

| 角色 | Lv.1 | Lv.2 | Lv.3 |
|---|---|---|---|
| 骑士 | 剑气射程与挥砍范围提升 | 剑气速度与挥砍击退提升 | 剑气末端释放破坏冲击波 |
| 法师 | 无敌或变羊持续时间提升 | 法球半径提升，无敌期间加速 | 施放技能获得两层护盾 |
| 蛮人 | 变大时间提升 | 碾压与推挤范围提升 | 单人回血门槛降低，双人强化持续时间和推力 |
| 游侠 | 翻滚速度提升 | 转向能力提升，翻滚期间无敌 | 单人翻滚可碾压，双人推力提升 |

## 道具与互动

### 增益道具

场上共有九种增益：

- 无敌
- 加速
- 滚滚
- 三段跳
- 星光磁铁
- 缩小蘑菇
- 云朵滑翔
- 自动冲刺
- 护盾

不同增益可以叠加，同类增益会刷新持续时间。所有速度倍率存在总上限，避免速度失控。

### 陷阱道具

- 香蕉皮：短时间内自动左右滑移。
- 致幻蘑菇：短时间内反转左右控制。
- 墨鱼：持续 8 秒的全屏黑雾，只保留奔跑角色的可见轮廓。
- 巨大蘑菇：体型放大至 500%，速度降低，但推挤力量增强。

负面效果最多同时存在三种，同类效果会刷新。

### 问号道具

双人模式中拾取问号后可以获得一次攻击充能，最多保存三层。按各自问号键向对手发射追踪问号球，命中后随机造成陷阱或增益效果。

## 天赋树

主页“天赋”入口包含两条成长分支：

- A 分支：落地、跳跃、空中控制、滞空和云朵平台速度。
- B 分支：星星拾取、金币双倍、蘑菇提示和结算额外星星。

天赋点通过完成单人或双人比赛获得。已解锁天赋会保存到浏览器本地。

## 存档与设置

浏览器本地保存的主要数据包括：

- `dreamrun_stats`：累计星光、星币、局数、金币、蘑菇和最佳成绩。
- `dreamrun_characters`：P1、P2 当前选择的角色。
- `dreamrun_upgrades`：四名角色的强化等级。
- `dreamrun_talents`：天赋点与已解锁节点。
- `dreamrun_settings`：静音状态和上次选择难度。

如果浏览器禁止本地存储，游戏会自动使用内存默认值，不会因此崩溃。

## 运行与部署

### 直接打开

直接双击 `index.html` 可以进入游戏，但浏览器可能拦截本地 GLTF 模型，角色会回退为程序化模型。

### 推荐本地服务

```bash
python3 -m http.server 8000
# 打开 http://localhost:8000/index.html
```

### 局域网联机服务

```bash
python3 server.py
python3 server.py --port 9000
```

macOS 也可以双击 `启动服务器.command`。程序只依赖 Python 标准库。

### 在线版本

GitHub Pages 托管静态页面，联机通过 Cloudflare Worker 和 Durable Object 房间中继。Room 只负责在同一房间内转发消息，不解析游戏逻辑。

## 技术说明

- 渲染：Three.js r160，支持本地资源和 CDN 回退。
- 角色：KayKit Adventurers CC0 GLB 模型。
- 物理：手写 60Hz 固定步长运动、球体和 AABB 碰撞。
- 网络：WebSocket JSON 消息，主机权威同步，客机无预测。
- 客户端：单个 `index.html`，无 npm、无构建步骤。
- 材质和特效：云、道具、粒子、描边和音效均由代码生成。

## 已知限制

- 在线联机依赖中继服务和网络连接。
- 局域网直连需要主机运行 `server.py`。
- 直接以 `file://` 打开时，浏览器可能阻止 GLB 模型加载。
- 联机客机没有客户端预测，本机操作存在约 30–60 毫秒的同步延迟。
- 当前主要面向桌面浏览器和键盘操作。

## 反馈与联系

游戏主页包含“联系作者”入口，可查看作者联系方式。问题反馈建议附带浏览器版本、模式、角色、难度和复现步骤。
